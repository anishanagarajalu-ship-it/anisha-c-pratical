  #include <stdio.h>

int main() {
  /* TODO: define the grades variable here */
  int grades[4];
  int average;

  grades[0] = 80;
  /* TODO: define the missing grade
     so that the average will sum to 85. */
  grades[1] = 85;
  grades[2] = 90;
  grades[3] = 75;

  average = (grades[0] + grades[1] + grades[2] + grades[3]) / 4;
  printf("The average of the 4 grades is: %d", average);

  return 0;
}                   
o/p:  The average of the 4 grades is: 82
