#include <stdio.h>
int main() {
    int i, n;

    printf("Enter number of students: ");
    scanf("%d", &n);

    printf("Attendance numbers are:\n");

    for (i = 1; i <= n; i++) {
        printf("%d\n", i);
    }

    return 0;
}
